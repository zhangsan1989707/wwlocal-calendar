# 2026-06-05 工作日志

## 修复移动端日历选中日期错位

**问题：** 移动端日历 6月5日选中状态下，蓝色圆形背景的日期数字与农历文字位置错位（重叠/不居中）

**修改文件：** `frontend/src/views/calendar/CalendarHome.vue`

**修改内容：**
1. `.mobile-day` 添加 flex 布局：`display: flex; flex-direction: column; align-items: center; justify-content: center; gap: 2px`
2. `.mobile-day span` 移除 `margin-top: -4px`（由 flex gap 替代）
3. `.mobile-day.active span` 的 `translateY(-24px)` 改为 `translateY(-2px)`，防止农历文字大幅上移与蓝色圆圈重叠

## 功能架构文档生成

**任务：** 全面审查项目代码架构，生成功能架构文档。

**审查范围：**
- 前端 32 个源文件（Vue SFC / TS / CSS）
- 后端 15 个 Java 源文件（Controller / Service / Config）
- 数据库 2 个 SQL 文件（init.sql + migrate-v2.sql）
- 部署配置（docker-compose.yml + nginx.conf）
- E2E 测试（Playwright 13 个验收用例）
- 现有文档（system_design.md / PRD / 需求清单）

**生成文档：** `docs/functional-architecture.md`
- 系统概述（双终端架构）
- 技术架构总览（ASCII 架构图）
- 数据库设计（18 张表 ER 关系 + 日历类型体系 + 关键约束）
- API 接口清单（33 个接口，按控制器分组）
- 前端架构（目录结构 / 路由 / 状态管理 / 响应式断点 / E2E 覆盖）
- 功能模块清单（用户端 22 项 + 管理端 17 项，均标注实现状态）
- 部署架构（Docker Compose 三容器 + 端口映射 + 关键配置）
- 关键设计决策（6 项架构决策及原因）
- 完整文件清单（含职责说明）
- V2 重大变更轨迹

**关键发现（代码更新审查）：**
1. Schema 层已完成从旧命名体系（sys_department/sys_user/calendar）到新标准命名（departments/users/calendars）的平滑迁移
2. 用户/部门/日历的 ID 从 BIGINT 迁移到 VARCHAR(36) UUID
3. 前端类型定义已与后端/DB 字段完全对齐（如 start_at/end_at/tag_id）
4. 假数据已全部清理，所有数据通过 API 获取
5. 移动端响应式设计完善（日/三日/周视图 + 设置面板 + 日历添加 Sheet）
6. E2E 测试覆盖 13 个验收用例，测试结果均有留存
7. 通用 AdminTablePage 组件复用度高，通过 props 驱动 7 个管理页面
