# 2026-06-05 工作日志

## 修复移动端日历选中日期错位

**问题：** 移动端日历 6月5日选中状态下，蓝色圆形背景的日期数字与农历文字位置错位（重叠/不居中）

**修改文件：** `frontend/src/views/calendar/CalendarHome.vue`

**修改内容：**
1. `.mobile-day` 添加 flex 布局：`display: flex; flex-direction: column; align-items: center; justify-content: center; gap: 2px`
2. `.mobile-day span` 移除 `margin-top: -4px`（由 flex gap 替代）
3. `.mobile-day.active span` 的 `translateY(-24px)` 改为 `translateY(-2px)`，防止农历文字大幅上移与蓝色圆圈重叠
