# 企业协同日历 H5 系统验证记录

## 一、文档信息

| 项目 | 内容 |
| --- | --- |
| 系统名称 | 企业协同日历 H5 系统 |
| 记录版本 | v1.0 |
| 验证日期 |  |
| 验证人员 |  |
| 部署环境 |  |
| 代码版本 |  |

## 二、编译验证

| 序号 | 验证对象 | 执行命令 | 预期结果 | 实际结果 | 结论 | 备注 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 后端服务 | `docker run --rm -v "$PWD/backend":/app -w /app maven:3.9-eclipse-temurin-17 mvn -DskipTests package` | 构建完成并生成后端制品 | 已生成 `backend/target/calendar-1.0.0.jar` | 通过 | 本机未安装 JDK 与 Maven，采用 Maven 容器完成 |
| 2 | 前端应用 | `cd frontend && npm run typecheck && npm run build` | 构建完成并生成前端静态文件 | 类型检查通过，已生成 `frontend/dist` | 通过 | Vite 输出 chunk 体积提示，不影响制品 |
| 3 | 前端容器镜像 | `docker build -t wwlocal-calendar-frontend:verify frontend` | 前端镜像构建完成 | 镜像构建完成 | 通过 | 依赖审计结果为 0 vulnerabilities |
| 4 | Compose 配置 | `docker compose -f deploy/docker-compose.yml config --quiet` | Compose 配置可解析 | 命令无错误输出 | 通过 | PostgreSQL 默认加载 `schema.sql` 与 `seed.sql` |

## 三、运行验证

| 序号 | 验证对象 | 执行方式 | 预期结果 | 实际结果 | 结论 | 备注 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 容器启动 | `cd deploy && docker compose up -d` | PostgreSQL、后端、前端服务均正常运行 | 未执行完整容器启动 | 待补充 | 后端镜像完整构建未单独执行，已完成 Maven 制品构建 |
| 2 | 前端访问 | 浏览器访问 `http://localhost:5173/calendar` | 日历首页可访问，页面资源加载正常 | 页面标题、创建日程、搜索、管理端入口可见 | 通过 | 使用 Vite 本地服务验证 |
| 3 | 管理端访问 | 浏览器访问 `http://localhost:5173/admin/all-calendars` | 全员日历管理可访问 | 管理端标题与“添加全员日历”按钮可见 | 通过 | 后端未启动时列表为空，页面可正常渲染 |
| 4 | 接口访问 | 访问 `/api` 下的服务接口 | Nginx 正确转发至后端服务 | 未执行完整容器启动 | 待补充 | Compose 配置已通过解析 |
| 4 | 附件目录 | 上传并访问附件文件 | 文件写入附件目录并可读取 |  |  |  |
| 5 | 备份目录 | 执行业务备份流程 | 备份文件写入备份目录 |  |  |  |

## 四、数据库验证

| 序号 | 验证对象 | 核验方式 | 预期结果 | 实际结果 | 结论 | 备注 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 表结构 | 执行 `database/schema.sql` | PRD 核心表创建完成 | 临时 PostgreSQL 16 容器执行成功 | 通过 | worker 已额外验证 `database/init.sql` 可在 PostgreSQL 16 执行 |
| 2 | 基础数据 | 执行 `database/seed.sql` 并查询用户、部门、日历、标签 | 8 名用户、3 个部门、个人/共享/公共/全员日历和标签数据完整 | 用户 8、部门 3、日历 11、日程 20、附件 3 | 通过 | 默认容器启动加载该脚本 |
| 3 | 日程数据 | 查询 `event`、`event_participant`、`event_recurrence` | 20 条日程及参会、重复、冲突相关数据完整 |  |  |  |
| 4 | 附件数据 | 查询 `event_attachment` | 附件记录完整，路径指向附件目录 |  |  |  |
| 5 | 审计与配置 | 查询 `audit_log`、`system_config`、`backup_record` | 审计、配置、备份恢复记录完整 |  |  |  |

## 五、业务流程验证

| 序号 | 流程 | 验证要点 | 实际结果 | 结论 | 备注 |
| --- | --- | --- | --- | --- | --- |
| 1 | 日历视图 | 日、周、月、列表视图切换，日程展示一致 |  |  |  |
| 2 | 日程维护 | 创建、查看、编辑、删除日程，保存后可查询 |  |  |  |
| 3 | 参会回执 | 接受、拒绝、待定、未响应状态可保存并回显 |  |  |  |
| 4 | 全员日历 | 自动订阅范围和共享成员可保存、回显并写入审计日志 |  |  |  |
| 5 | 搜索筛选 | 按标题、地点、参会人、日历、标签和时间范围查询 |  |  |  |
| 6 | 导出任务 | 创建导出任务并生成下载文件 |  |  |  |

## 六、备份恢复验证

| 序号 | 验证对象 | 核验方式 | 预期结果 | 实际结果 | 结论 | 备注 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 数据库备份 | 执行备份流程并查看文件 | 数据库备份文件生成 |  |  |  |
| 2 | 附件备份 | 执行备份流程并查看文件 | 附件备份包生成 |  |  |  |
| 3 | 恢复登记 | 登记恢复结果 | 恢复记录写入 `backup_record` |  |  |  |
| 4 | 恢复后核验 | 查询用户、日历、日程、附件、待办和审计日志 | 核心数据保持完整 |  |  |  |

## 七、问题记录

| 序号 | 问题描述 | 影响范围 | 处理方式 | 处理结果 | 负责人 |
| --- | --- | --- | --- | --- | --- |
| 1 |  |  |  |  |  |

## 八、验证结论

| 项目 | 内容 |
| --- | --- |
| 编译验证结论 |  |
| 运行验证结论 |  |
| 数据库验证结论 |  |
| 备份恢复验证结论 |  |
| 总体结论 |  |
| 验证人员签字 |  |
| 确认日期 |  |
